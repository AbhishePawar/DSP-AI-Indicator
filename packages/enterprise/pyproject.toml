[build-system]
requires = ["setuptools>=69.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "enterprise"
version = "0.1.0"
description = "DSP Enterprise Commercial Platform foundation (EPS-002 / EPIC-016–019)"
requires-python = ">=3.11"
license = { text = "Proprietary" }
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
