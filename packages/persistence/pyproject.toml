[build-system]
requires = ["setuptools>=69.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "persistence"
version = "0.1.0"
description = "DSP Institutional Persistence Layer (EPIC-A008) — metadata & references only"
requires-python = ">=3.11"
license = { text = "Proprietary" }
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
