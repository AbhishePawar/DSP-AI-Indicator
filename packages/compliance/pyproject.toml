[build-system]
requires = ["setuptools>=69.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "compliance"
version = "0.2.0"
description = "DSP Compliance — Research Mode, DPDP consent, disclosures, history (PR1.0 + PEP-004)"
requires-python = ">=3.11"
license = { text = "Proprietary" }
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
