{
  "name": "DSP AI Indicator",
  "install": "bash scripts/bootstrap/cloud-agent-install.sh",
  "terminals": [
    {
      "name": "api",
      "command": "python3 -m uvicorn api_platform.api.app:app --host 0.0.0.0 --port 8000",
      "description": "FastAPI backend (frozen /api/v1) served by uvicorn on port 8000."
    },
    {
      "name": "web",
      "command": "cd apps/web && npm run dev",
      "description": "Next.js thin-client web app (dev server) on port 3000; talks to the API at /api/v1."
    }
  ],
  "ports": [
    { "name": "api", "port": 8000 },
    { "name": "web", "port": 3000 }
  ]
}
