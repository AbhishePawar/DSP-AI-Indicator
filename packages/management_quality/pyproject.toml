[build-system]
requires = ["setuptools>=69.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "management_quality"
version = "0.1.0"
description = "Management Quality & Capital Allocation Intelligence for DSP AI Indicator (FEATURE-002)"
requires-python = ">=3.11"
license = { text = "Proprietary" }
dependencies = [
    "core",
    "financial",
    "business_quality",
]

[tool.setuptools.packages.find]
where = ["src"]
