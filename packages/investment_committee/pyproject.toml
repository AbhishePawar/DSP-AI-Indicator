[build-system]
requires = ["setuptools>=69.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "investment_committee"
version = "0.1.0"
description = "Deterministic AI Committee Consensus Engine for DSP AI Indicator (FEATURE-008)"
requires-python = ">=3.11"
license = { text = "Proprietary" }
dependencies = [
    "core",
    "valuation",
    "economic_moat",
    "management_quality",
    "financial_strength",
    "earnings_quality",
    "growth_quality",
    "business_quality_aggregator",
    "investment_recommendation",
]

[tool.setuptools.packages.find]
where = ["src"]
